export const metadata = {
     title: "Tours Page",
     description: "Tours page in Combined Tours and Travels",
};

const ContactLayout = ({ children }) => {
     return <div>{children}</div>;
};

export default ContactLayout;
